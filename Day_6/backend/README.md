# Day 6 – Express Middleware

Learned the concept of middleware and request lifecycle in Express.

Implemented:
- Logger middleware that logs request time, method, and URL
- Router middleware for admin route protection
- Built-in middleware (express.json)
- Third-party middleware (cors)

Example log output:
10:30:22 GET /users